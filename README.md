# Kotlin Spring Boot simple CRUD

- `POST /entities`
  - body = `{"name": "name","description": "description"}`
- `GET /entities/{id}`
- `GET /entities`
- `PUT /entities/{id}`
  - body = `{"name": "name","description": "description"}`
- `DELETE /entities/{id}`